schema:
  - https://rickandmortyapi.com/graphql
  - src/schema/local-extensions.graphql

documents:
  - src/**/*.graphql

generates:
  ./src/types/base-types.ts:
    plugins:
      - typescript
      - typed-document-node

  ./src/:
    preset: near-operation-file
    presetConfig:
      folder: false
      baseTypesPath: ./types/base-types.ts
      extension: .generated.ts
    plugins:
      - typescript-operations
      - typescript-react-apollo
    config:
      withHooks: true
      useIndexSignature: trueр